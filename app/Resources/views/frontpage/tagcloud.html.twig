<div id="tag-cloud" class="widget">
    <h3>Nuestros tags</h3>
    <div class="tags">
{% for tag in tags %}
      <a class="url" href="{{ path(tag.tag) }}">{{ netgen_tags_tag_keyword(tag.tag) }}</a> ({{ tag.count }}){% if not loop.last %}, {% endif %}
{% endfor %}...
    </div>
</div>
