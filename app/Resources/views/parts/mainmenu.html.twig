
    <div id="mainmenu" class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
            {% for location in locationList %}
            <li id="menu-item-{{ location.id }}" class="menu-item menu-item-{{ location.id }}{% if location.id == selected %} active{% endif %}"><a title="{{ location.contentInfo.name }}" href="{{ path( location ) }}">{{ location.contentInfo.name }}</a></li>
            {% endfor %}
        </ul>
    </div><!--/.nav-collapse -->
